const bcrypt=require('bcryptjs');const connect=require('./connect');const {User,Outlet,Category,Product,OutletProduct,Setting}=require('../models');
(async()=>{await connect();const admin=await User.findOneAndUpdate({email:process.env.ADMIN_BOOTSTRAP_EMAIL||'admin@mrbreado.local'},{$setOnInsert:{name:'Mr Breado Admin',passwordHash:await bcrypt.hash(process.env.ADMIN_BOOTSTRAP_PASSWORD||'ChangeMe123!',12),role:'ADMIN',active:true}},{upsert:true,new:true});const outlet=await Outlet.findOneAndUpdate({slug:'mr-breado-main'},{$setOnInsert:{name:'Mr Breado Main',slug:'mr-breado-main',code:'MB-MAIN',primary:true,active:true,open:true,location:{type:'Point',coordinates:[88.3639,22.5726]},deliveryRadiusKm:10,createdBy:admin._id}},{upsert:true,new:true});const cat=await Category.findOneAndUpdate({slug:'popular'},{$setOnInsert:{name:'Popular',slug:'popular',active:true}},{upsert:true,new:true});const p=await Product.findOneAndUpdate({slug:'sample-burger'},{$setOnInsert:{name:'Sample Burger',slug:'sample-burger',sku:'MB-SAMPLE-001',categoryId:cat._id,basePrice:99,active:true,createdBy:admin._id}},{upsert:true,new:true});await OutletProduct.updateOne({outletId:outlet._id,productId:p._id},{$setOnInsert:{stockQuantity:100,lowStockThreshold:10,enabled:true,available:true}},{upsert:true});await Setting.updateOne({key:'feature_toggles'},{$setOnInsert:{value:{onlinePayment:true,cod:true,takeaway:true,delivery:true,riderAssignment:true,offers:true},public:true,active:true}},{upsert:true});console.log('Seed complete');process.exit(0)})().catch(e=>{console.error(e);process.exit(1)});
