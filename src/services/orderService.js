const mongoose=require('mongoose');const {nanoid}=require('nanoid');const {Outlet,Product,OutletProduct,Order,OrderEvent,Payment,Refund,RiderEarning}=require('../models');const settings=require('./settingsService');const inventory=require('./inventoryService');const {haversineKm,deliveryCharge}=require('../utils/geo');const {AppError}=require('../utils/errors');
const transitions={PENDING_PAYMENT:['RECEIVED','PAYMENT_FAILED','CANCELLED'],RECEIVED:['ACCEPTED','REJECTED','CANCELLED'],ACCEPTED:['PREPARING','CANCELLED'],PREPARING:['READY','CANCELLED'],READY:['RIDER_ASSIGNMENT_PENDING','RIDER_ASSIGNED','PICKED_UP','DELIVERED','CANCELLED'],RIDER_ASSIGNMENT_PENDING:['RIDER_ASSIGNED','CANCELLED'],RIDER_ASSIGNED:['PICKED_UP','CANCELLED'],PICKED_UP:['DELIVERED'],REJECTED:[],CANCELLED:[],DELIVERED:[],REFUND_PENDING:['REFUNDED'],PAYMENT_FAILED:[]};
const aliases={PLACED:'RECEIVED',CONFIRMED:'ACCEPTED',PREPARED:'READY',OUT_FOR_DELIVERY:'PICKED_UP',COMPLETED:'DELIVERED'};
const canonical=s=>aliases[String(s||'').toUpperCase()]||String(s||'').toUpperCase();
async function buildPricing({outletId,items,address,fulfilmentType='DELIVERY',couponCode}){const type=String(fulfilmentType||'DELIVERY').toUpperCase();if(!['DELIVERY','TAKEAWAY'].includes(type))throw new AppError('Invalid fulfilment type',400,'INVALID_FULFILMENT_TYPE');const features=await settings.getBusinessFeatures();if(type==='DELIVERY'&&!features.feature_toggles.delivery)throw new AppError('Delivery is currently disabled',409,'DELIVERY_DISABLED');if(type==='TAKEAWAY'&&!features.feature_toggles.takeaway)throw new AppError('Takeaway is currently disabled',409,'TAKEAWAY_DISABLED');const outlet=await Outlet.findById(outletId).lean();if(!outlet||!outlet.active||!outlet.open)throw new AppError('Outlet unavailable',409,'OUTLET_UNAVAILABLE');const ids=items.map(x=>x.productId);const rows=await OutletProduct.find({outletId,productId:{$in:ids},enabled:true,available:true}).populate('productId').lean();const map=new Map(rows.map(x=>[String(x.productId._id),x]));let subtotal=0;const snapshots=items.map(i=>{const r=map.get(String(i.productId));if(!r||!r.productId?.active||r.stockQuantity-r.reservedQuantity<Number(i.quantity))throw new AppError('One or more foods are unavailable or out of stock',409,'STOCK_UNAVAILABLE');const p=r.productId;const price=Number(r.offerPriceOverride??r.priceOverride??(p.offerPrice>0?p.offerPrice:p.basePrice));const qty=Number(i.quantity);const custom=(i.customizations||[]).reduce((a,c)=>a+Number(c.price||0),0);const total=(price+custom)*qty;subtotal+=total;return{productId:p._id,name:p.name,slug:p.slug,sku:p.sku,image:p.images?.[0]?.url,quantity:qty,unitPrice:price,offerPrice:price,tax:0,customizations:i.customizations||[],finalTotal:Number(total.toFixed(2))};});
const taxSettings=await settings.get('tax');const tax=Number((subtotal*Number(taxSettings?.rate||0)/100).toFixed(2));let distanceKm=0,delCharge=0;if(type==='DELIVERY'){if(address?.latitude==null||address?.longitude==null)throw new AppError('Delivery coordinates required');const [lng,lat]=outlet.location.coordinates;distanceKm=Number(haversineKm(lat,lng,Number(address.latitude),Number(address.longitude)).toFixed(2));if(distanceKm>outlet.deliveryRadiusKm)throw new AppError('Address is outside outlet delivery radius',409,'OUT_OF_RANGE');const ds=outlet.deliverySettings||await settings.get('delivery');delCharge=deliveryCharge(distanceKm,ds);}const total=Number((subtotal+tax+delCharge).toFixed(2));const takeawayAdvancePercentage=type==='TAKEAWAY'?Number(features.takeaway.advanceValue||0):0;const payableOnlineAmount=type==='TAKEAWAY'?Number((total*takeawayAdvancePercentage/100).toFixed(2)):total;const balanceDue=type==='TAKEAWAY'?Number((total-payableOnlineAmount).toFixed(2)):0;return{outlet,snapshots,subtotal:Number(subtotal.toFixed(2)),discount:0,tax,deliveryCharge:delCharge,total,distanceKm,fulfilmentType:type,takeawayAdvancePercentage,payableOnlineAmount,balanceDue,featureToggles:features.feature_toggles};}
async function createOrder({customerId,outletId,items,address,fulfilmentType,paymentMethod,clientRequestId}){const existing=clientRequestId?await Order.findOne({clientRequestId}):null;if(existing)return existing;const pricing=await buildPricing({outletId,items,address,fulfilmentType});const method=String(paymentMethod||'COD').toUpperCase();if(method==='ONLINE'&&!pricing.featureToggles.onlinePayment)throw new AppError('Online payment is currently disabled',409,'ONLINE_PAYMENT_DISABLED');if(method==='COD'&&!pricing.featureToggles.cod)throw new AppError('Cash on delivery is currently disabled',409,'COD_DISABLED');if(pricing.fulfilmentType==='TAKEAWAY'&&pricing.takeawayAdvancePercentage>0&&!pricing.featureToggles.onlinePayment)throw new AppError('Takeaway requires online advance payment, but online payment is disabled',409,'TAKEAWAY_PAYMENT_UNAVAILABLE');if(pricing.fulfilmentType==='TAKEAWAY'&&pricing.takeawayAdvancePercentage>0&&!['ONLINE','TAKEAWAY_ADVANCE'].includes(method))throw new AppError(`Takeaway requires ${pricing.takeawayAdvancePercentage}% online advance payment`,409,'TAKEAWAY_ADVANCE_REQUIRED');const effectiveMethod=pricing.fulfilmentType==='TAKEAWAY'&&pricing.takeawayAdvancePercentage>0?'TAKEAWAY_ADVANCE':method;const requiresOnline=effectiveMethod==='ONLINE'||effectiveMethod==='TAKEAWAY_ADVANCE';const session=await mongoose.startSession();let order;try{await session.withTransaction(async()=>{[order]=await Order.create([{slug:`MB-${Date.now()}-${nanoid(6)}`,clientRequestId,customerId,outletId,items:pricing.snapshots,address,fulfilmentType:pricing.fulfilmentType,paymentMethod:effectiveMethod,paymentStatus:'PENDING',status:requiresOnline?'PENDING_PAYMENT':'RECEIVED',subtotal:pricing.subtotal,discount:pricing.discount,tax:pricing.tax,deliveryCharge:pricing.deliveryCharge,total:pricing.total,payableOnlineAmount:requiresOnline?pricing.payableOnlineAmount:0,paidAmount:0,balanceDue:pricing.fulfilmentType==='TAKEAWAY'?pricing.balanceDue:0,takeawayAdvancePercentage:pricing.takeawayAdvancePercentage,distanceKm:pricing.distanceKm,sellerAcceptanceDeadline:new Date(Date.now()+30*60000)}],{session});await inventory.reserve(pricing.snapshots,outletId,order._id,customerId,session,`order:${order._id}`);await OrderEvent.create([{orderId:order._id,previousStatus:null,newStatus:order.status,actorType:'CUSTOMER',actorId:customerId,metadata:{fulfilmentType:pricing.fulfilmentType,takeawayAdvancePercentage:pricing.takeawayAdvancePercentage,payableOnlineAmount:requiresOnline?pricing.payableOnlineAmount:0,balanceDue:pricing.balanceDue},idempotencyKey:`order:${order._id}:created`}],{session});});}finally{await session.endSession();}return order;}
async function changeStatus(order,user,nextStatus,reason,idempotencyKey){const next=canonical(nextStatus),prev=canonical(order.status);if(prev===next)return order;if(!(transitions[prev]||[]).includes(next))throw new AppError(`Invalid transition ${prev} -> ${next}`,409,'INVALID_STATUS_TRANSITION');const session=await mongoose.startSession();await session.withTransaction(async()=>{order.status=next;if(next==='ACCEPTED')order.acceptedAt=new Date();if(next==='READY')order.readyAt=new Date();if(next==='PICKED_UP')order.pickedUpAt=new Date();if(next==='DELIVERED'){order.deliveredAt=new Date();await inventory.consume(order.items,order.outletId,order._id,user.id,session,`order:${order._id}`);if(order.riderId){const rate=Number((await settings.get('rider'))?.perKmRate||0);await RiderEarning.updateOne({orderId:order._id},{$setOnInsert:{riderId:order.riderId,orderId:order._id,outletId:order.outletId,distanceKm:order.distanceKm,ratePerKm:rate,amount:Number((order.distanceKm*rate).toFixed(2)),status:'PENDING'}},{upsert:true,session});}}if(['CANCELLED','REJECTED','PAYMENT_FAILED'].includes(next)){order.cancelledAt=new Date();order.cancellationReason=reason;await inventory.release(order.items,order.outletId,order._id,user.id,session,`order:${order._id}`);if(order.paymentStatus==='SUCCESS'){const payment=await Payment.findOne({orderId:order._id,status:'SUCCESS'}).session(session);if(payment){await Refund.updateOne({orderId:order._id,paymentId:payment._id},{$setOnInsert:{customerId:order.customerId,outletId:order.outletId,amount:payment.amount,cancellationReason:reason,status:'PENDING'}},{upsert:true,session});order.refundStatus='PENDING';}}}await order.save({session});await OrderEvent.create([{orderId:order._id,previousStatus:prev,newStatus:next,actorType:user.role,actorId:user.id,reason,idempotencyKey:idempotencyKey||`order:${order._id}:${prev}:${next}`}],{session});});await session.endSession();return order;}
module.exports={buildPricing,createOrder,changeStatus,canonical,transitions};
